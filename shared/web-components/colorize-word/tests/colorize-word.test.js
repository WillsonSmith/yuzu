import { html } from 'lit';
import { fixture, expect } from '@open-wc/testing';

// import '../colorize-word.js';

describe(`<colorize-word>`, () => {
  // colorize-word uses javascript-color-gradient which fails to resolve.

  // let element;
  // beforeEach(async() => {
  //   element = fixture(html`<colorize-word>Hello</colorize-word>`);
  // });

  // it(`should render`, () => {
  //   const domLetters = element.shadowRoot.querySelectorAll(`span`);
  //   expect(domLetters.length).to.equal(5);
  // });
});
